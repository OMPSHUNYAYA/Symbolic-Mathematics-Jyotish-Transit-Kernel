SSM-JA Verification — Deterministic Observational Realization

1. Open the standalone observational atlas

Option A (recommended)

Open:

demo\SSM-JA_v3_3_11.html

directly in a modern browser.


Option B

Open from repository root:

demo/SSM-JA_v3_3_11.html

Supported range:
01 Jan 1950 -> 31 Dec 2100


2. Observe

deterministic offline observational realization
no runtime ephemeris dependency during execution
no cloud dependency
no runtime CSV loading
no external astronomical API dependency
no server-side realization dependency
browser-local deterministic realization
embedded sidereal kernel realization
deterministic Panchang realization
deterministic Dasha continuity realization
deterministic timezone-aware chart realization

chart realization is resolved from embedded deterministic structure

correctness = resolve(structure)


3. Expected baseline behavior

valid supported input

  chart_output: RESOLVED
  realization_visible: True
  deterministic_output: True
  integrity_state: PASS

same supported input

  identical chart structure
  identical Dasha structure
  identical Panchang realization
  identical integrity state

invalid or incomplete input

  realization blocked
  missing required fields identified


4. Structural realization rule

embedded_structure_complete = TRUE
AND
embedded_structure_consistent = TRUE

observational_realization_visible iff
embedded_structure_complete
AND
embedded_structure_consistent

jyotish_output = resolve(embedded_sidereal_kernel_structure)


5. Expected behavior

same input
-> same chart output

same input
-> same Dasha realization

same input
-> same Panchang realization

same release
-> same deterministic observational structure

offline execution
-> identical deterministic realization

runtime dependency variation
does not determine observational correctness


6. Verify deterministic realization

Open the same HTML twice.

Use identical:

- birth input
- timezone
- location
- calculation settings

Expected:

same Rasi chart
same Navamsa chart
same Nakshatra
same Dasha structure
same Panchang realization
same integrity state

Confirm:

same input -> same deterministic chart structure


6.1 Verify deterministic continuity

Repeat identical calculations across multiple sessions.

Expected:

identical observational realization
identical Dasha boundaries
identical Panchang continuity
identical deterministic output structure


7. Verify representative observational cases

Case 1 — Standard supported chart

Expected:

  chart_output: RESOLVED
  realization_visible: True
  deterministic_output: True


Case 2 — Panchang realization

Expected:

  deterministic Tithi
  deterministic Yoga
  deterministic Karana
  deterministic Paksha
  stable sunrise/sunset realization


Case 3 — Vimshottari Dasha realization

Expected:

  deterministic Mahadasha structure
  deterministic Antardasha structure
  deterministic Pratyantardasha structure
  stable timestamp continuity


Case 4 — Repeat execution determinism

Expected:

  first realization == second realization
  deterministic match: True


Case 5 — Offline execution

Recommended browsers for local file execution: Chrome, Firefox, or Edge.

Some browser configurations may restrict local file access under strict security policies.
If the file does not load, try a different modern browser or check local file access settings.

Expected:

  no internet dependency
  no external ephemeris fetch
  no runtime API dependency
  identical local realization


8. Verify deterministic observational identity

Different inputs:

different birth data
different timezone
different location
different timestamps

Expected:

different observational realization

Confirm:

different input structure
-> different chart realization


9. Verify incomplete-input safety

Remove required input fields.

Expected:

realization blocked
required input fields identified


10. Verify timezone transparency

Change timezone offset manually.

Example:

Enter a chart with timezone `+05:30` (India Standard Time).
Then re-enter the same birth data with timezone `+00:00` (UTC).

Expected:

observable chart realization changes
observable Panchang changes (sunrise/sunset shifts by approximately 5h 30m)
observable Dasha continuity variation where applicable

Confirm:

timezone realization is explicit and user-verifiable
chart output changes strictly follow the timezone input


11. Verify offline execution independence

Disconnect internet completely.

Expected:

full chart realization remains functional
full Panchang realization remains functional
full Dasha realization remains functional

Confirm:

runtime internet dependency does not exist


12. Verify deterministic consistency
under repeated execution

Same:

- input
- timezone
- release version
- embedded kernel

Expected:

same realization
same observational structure

Note:

runtime execution ordering
does not determine chart correctness


13. Verify structural transitions

Change input structure:

valid supported input
incomplete input
timezone variation
location variation

Expected:

realization changes strictly follow input structure


14. Verify embedded kernel integrity

Observe runtime integrity state.

Expected:

  integrity_state: PASS

Meaning:

embedded kernel matches frozen release identity


15. Verify release identity

Verify the frozen SHA-256 release hash.

Expected:

same HTML file
-> same SHA-256 hash

different HTML file
-> different SHA-256 hash

In the reference implementation:

SHA-256 is used for deterministic release verification.


16. Verify file identity

Windows:

certutil -hashfile demo\SSM-JA_v3_3_11.html SHA256

macOS / Linux:

sha256sum demo/SSM-JA_v3_3_11.html

Expected output (all platforms):

hash matches:

VERIFY/FREEZE_DEMO_SHA256.txt

2adffbecdc894f1be3611962fc1b96e4d983e996d5c070d4b4e955ed92b039d8


Note:

file hash = release identity

the embedded deterministic kernel
is part of the release identity

17. Observational realization model

resolve(structure) ->

RESOLVED      if supported deterministic structure is complete
BLOCKED       if required input structure is incomplete


18. Core invariant

jyotish_output = resolve(structure)

observational_realization_visible iff
required structure is complete
AND
embedded deterministic kernel remains consistent

same input
-> same chart output

same release
-> same deterministic realization

Correctness claims apply only within
the supported observational release range.


19. Confirm

identical input structure
-> identical deterministic realization

identical embedded release
-> identical observational continuity

no runtime ephemeris dependency during execution

no cloud dependency

no runtime CSV dependency

no external astronomical API dependency

offline browser execution remains reproducible

invalid or incomplete structure
does not produce observational realization


Result

Deterministic observational realization confirmed for JA or SSM-JA.

Bounded observational correctness is resolved from embedded deterministic structure —
not from runtime ephemeris streaming,
cloud realization systems,
runtime interpolation dependency,
external astronomical APIs,
or server-side realization infrastructure.
